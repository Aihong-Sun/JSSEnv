language: python
python:
  - "3.8"
install:
  - python3 -m pip install kaleido
  - pip install -e .
script:
  - cd tests/non_fexible
  - pytest
  - cd ../flexible
  - pytest
after_success:
  - codecov